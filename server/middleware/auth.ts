export default defineEventHandler(async (event) => {
   
    
    // Return a JSON response with a message and the query parameters
    // return {
    //     message: 'i am middleware',
    //     // query: query,
    // };
}
);